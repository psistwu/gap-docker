services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ALPINE_VERSION: ${ALPINE_VERSION}
        GAP_VERSION: ${GAP_VERSION}
    image: gap-docker:${GAP_VERSION}
    container_name: gap-app
    command: tail -f /dev/null