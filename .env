# The version of GAP to run through docker compose.
# You can set this to a specific version (e.g., "4.11.1") or "latest" to always get the latest version.
GAP_VERSION=latest

WORKDIR=/workspace

# Please set the following environment variable to a secure token before running the container
JUPYTER_TOKEN=mytoken