# gap-docker
This is a docker environment for GAP system, including a `Dockerfile`
that builds the docker image and a `docker-compose.yaml` that initiates
a docker container in which GAP-system runs.
